import React from 'react';


class SetListSongCardContainer extends React.Component {
  constructor() {
    super()

  }

  render() {
    //Here, grab all of your set-songs from props. Order them by their set order. Then render out band cards based on the order number. Each band card needs to display its order in the set and be able to increment and decrement it. All relevant cards need to change accordingly. Oofs. How would you write this logic out dynamically?
    return (

    )
  }
}
